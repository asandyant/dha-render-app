<%- include('partials/header', { title: 'Signature saved', heading: 'You are signed in', subheading: 'Your signature has been added to today's DHA.' }) %>

<div class="card">
  <div style="font-size:28px;font-weight:800;margin-bottom:10px;">Thanks, <%= signerName %>.</div>
  <div class="muted">Your signature is now on the crew sign-in section of today's DHA.</div>
  <div class="count-pill" style="margin-top:18px;display:inline-flex;"><%= progress.progressText %></div>
</div>

<%- include('partials/footer') %>
