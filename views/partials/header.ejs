<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title %></title>
  <link rel="stylesheet" href="/styles.css" />
</head>
<body>
  <header class="app-header no-print">
    <div class="container header-row">
      <div>
        <div style="font-size:28px;font-weight:800;"><%= heading %></div>
        <% if (subheading) { %><div class="brand-sub"><%= subheading %></div><% } %>
      </div>
      <% if (showHeaderActions !== false) { %>
      <div class="actions">
        <a class="btn primary" href="/foreman/start-today">Start Today's DHA</a>
      </div>
      <% } %>
    </div>
  </header>
  <main class="page-wrap">
    <div class="container">
